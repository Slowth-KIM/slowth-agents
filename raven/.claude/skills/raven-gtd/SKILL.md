---
name: raven-gtd
description: GTD 기반 태스크 매니저를 시작합니다. 태스크 캡처, inbox 정리, focus 관리, weekly review를 수행합니다.
---

# Raven GTD - Task Manager

GTD(Getting Things Done) 기반 태스크 매니저를 시작합니다.

## 시작하기

1. 먼저 `.raven/` 폴더가 있는지 확인합니다
2. 없으면 초기화 안내를 제공합니다
3. 있으면 GTD 에이전트를 실행합니다

## 실행 지침

다음 순서로 진행하세요:

### 1. 초기화 확인

`.raven/state/project.json` 파일이 존재하는지 확인하세요.

**없는 경우:**
```
🪶 Raven이 초기화되지 않았습니다.

초기화하려면 다음 명령을 실행하세요:
  bash tools/init-raven.sh

또는 수동으로 .raven/ 구조를 생성할 수 있습니다.
```

### 2. 상태 로드

`project.json`에서 현재 상태를 로드하고 태스크 수를 카운트합니다:

```bash
# 각 폴더의 태스크 수 카운트
for folder in inbox focus next someday done; do
  count=$(find .raven/tasks/$folder -name "*.md" 2>/dev/null | wc -l)
  echo "$folder: $count"
done
```

### 3. 메뉴 표시

상태 요약과 함께 메뉴를 표시합니다:

```
📥 Inbox: {n}개  🎯 Focus: {n}/3  📋 Next: {n}개  💭 Someday: {n}개  ✅ Done: {n}개

무엇을 할까요?
[1] inbox - Inbox 정리 (Clarify)
[2] focus - Focus 관리 (현재 작업)
[3] add - 빠른 추가 (Capture)
[4] review - Weekly Review
[5] status - 전체 현황 보기
[x] 종료
```

### 4. 명령 처리

사용자 선택에 따라 `.claude/agents/raven-gtd.md`에 정의된 명령을 실행합니다.

## 인자 처리

skill 호출 시 인자가 있으면 바로 해당 명령 실행:

- `/raven-gtd inbox` → inbox 정리 바로 시작
- `/raven-gtd add "새 태스크"` → 바로 태스크 추가
- `/raven-gtd status` → 상태만 표시
- `/raven-gtd review` → weekly review 시작

## 핸드오프

- PRD가 필요한 태스크 → `/raven-init` 안내
- 구현 준비된 태스크 → `/raven-code` 안내
