---
description: "Cancel active autonomous loop"
allowed-tools: ["Bash(${CLAUDE_PLUGIN_ROOT}/scripts/cancel-raven-loop.sh:*)"]
---

# Raven Cancel Loop

Cancel the currently active autonomous loop:

```!
"${CLAUDE_PLUGIN_ROOT}/scripts/cancel-raven-loop.sh"
```
